# Document Flow Regression Evals

Document flow regression evals cover the seeded document project and the critical user tasks that must remain stable.

At minimum, the eval suite should cover tree loading, document open, title sync, lock behavior, comment flows, tab restore, empty-project draft behavior, and the regression outputs needed to debug failures quickly.
