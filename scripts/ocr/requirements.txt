# PaddleOCR-VL Dependencies
# 
# Note: For GPU support, replace paddlepaddle with:
#   pip install paddlepaddle-gpu==3.2.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/

# PaddlePaddle core (CPU version by default)
paddlepaddle>=3.0.0

# PaddleOCR with document parser support
paddleocr[doc-parser]>=3.0.0

# HTTP server dependencies
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.9

# Image processing
Pillow>=10.0.0

# PDF handling
PyMuPDF>=1.24.0

# JSON handling
orjson>=3.10.0
