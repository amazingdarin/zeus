# PaddleOCR-VL Dependencies
# 
# IMPORTANT: PaddlePaddle must be installed separately from official source:
#
#   CPU version:
#     pip install paddlepaddle -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
#
#   GPU version (CUDA 12.6):
#     pip install paddlepaddle-gpu -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
#
# Then install this file:
#     pip install -r requirements.txt

# PaddleOCR with document parser support
paddleocr[doc-parser]>=3.0.0

# HTTP server dependencies
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.9

# Image processing
Pillow>=10.0.0

# PDF handling
PyMuPDF>=1.24.0

# JSON handling
orjson>=3.10.0
