# PaddleOCR-VL Dependencies
# 
# IMPORTANT: PaddlePaddle must be installed first from official source.
# See: https://www.paddlepaddle.org.cn/install/quick
#
# Installation steps:
#   1. Install PaddlePaddle:
#
#      GPU (CUDA 12.6):
#        python -m pip install paddlepaddle-gpu==3.2.1 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
#
#      CPU:
#        python -m pip install paddlepaddle==3.2.1 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
#
#   2. Install PaddleOCR with doc-parser:
#        python -m pip install -U "paddleocr[doc-parser]"
#
#   3. Install other dependencies:
#        pip install -r requirements.txt
#
# Note: paddleocr[doc-parser] is NOT listed below as it requires special installation.

# HTTP server dependencies
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.9

# Image processing
Pillow>=10.0.0

# PDF handling
PyMuPDF>=1.24.0

# JSON handling
orjson>=3.10.0
