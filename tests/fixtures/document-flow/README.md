# Document Flow Fixtures

This directory stores seed inputs for the document flow harness.

Fixtures should define stable project, document, comment, and lock states so document flow regression runs are deterministic across worktrees and local environments.
