# Plugin Fixtures

This directory documents the plugin assumptions used by harnesses.

The first-pass skeleton relies on the Playwright automation account and expects `ppt-plugin` to be installable for the seeded personal project used by document-flow harnesses.
