# Project Scope Playwright Harness

This directory contains browser-level project-scope regressions.

The first personal-scope harness verifies that a frontend `projectRef` written to local storage leads to the expected owner-scoped request path when the browser triggers project-scoped data loading.
