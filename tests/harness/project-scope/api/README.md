# Project Scope API Harness

This directory contains API-level auth and owner-scope smoke checks.

The first-pass skeleton validates login, personal project listing, and invalid owner rejection so later team-scope harnesses can build on a stable base.
