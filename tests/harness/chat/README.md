# Chat Harness

This directory contains stable harness entrypoints for the Zeus chat domain.

Chat harnesses should verify seeded project selection, session lifecycle, streaming-related readiness, and browser-level chat page regression flows without depending on ad-hoc local state.
