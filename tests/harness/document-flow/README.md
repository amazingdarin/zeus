# Document Flow Harness

This directory contains the durable harness entrypoints for the Zeus document flow.

The goal is to keep document flow regression assets separate from ad-hoc debugging scripts. Every stable document flow regression should live here, use seeded fixtures, and write artifacts to a predictable regression output location.
