# Document Flow Playwright Harness

This directory contains browser-based document flow regression harnesses.

Each regression should target one user-visible document flow behavior, consume seeded document flow data, and produce reproducible artifacts for regression debugging.
