# Plugin Runtime Playwright Harness

This directory contains browser-level plugin runtime regressions.

These checks should verify that the plugin runtime list is reachable from the browser and that plugin runtime bootstrap errors remain visible in harness artifacts when they occur.
