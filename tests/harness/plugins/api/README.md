# Plugin Runtime API Harness

This directory contains API-level plugin runtime smoke checks.

The first-pass skeleton validates plugin install state, runtime listing, and project-scoped local-data operations for a seeded personal project.
