# Plugin Runtime Harness

This directory contains stable harness entrypoints for Zeus plugin runtime behavior.

Plugin harnesses should validate install state, runtime metadata, local-data contracts, and browser-side runtime loading assumptions without depending on manual local setup.
